import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { html } from 'lit-html';
import '../alert.ts';

<Meta
    title="Alert"
    args={{
        type: 'info',
        text: 'Lorem ipsum dolor sit amet, consectetur adipiscing elit',
        title: '',
        fill: true,
        border: 'none',
    }}
    argTypes={{
        type: {
            control: 'select',
            options: ['info', 'error', 'warning', 'success'],
        },
        border: {
            control: 'select',
            options: ['none', 'top', 'left'],
        },
    }}
/>

<Canvas>
    <Story name="Page">
        {({ type, text, title, fill, border }) =>
            html`<sw-alert
                type=${type}
                title=${title}
                ?fill=${fill}
                border=${border}
                >${text}</sw-alert
            >`
        }
    </Story>
</Canvas>
