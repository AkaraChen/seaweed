{
  "name": "dumi",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "@akrc/seaweed": "workspace:^0.0.3",
    "dumi": "^2.0.10"
  },
  "scripts": {
    "dev": "pnpm run prebuild && dumi dev",
    "prebuild": "pnpm run -w build:package",
    "prepare": "dumi setup",
    "build": "pnpm run prebuild && dumi build"
  }
}
