{
  "name": "dumi",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "@akrc/seaweed": "workspace:^0.0.3",
    "dumi": "^2.0.13",
    "lit": "2.5.0",
    "react": "18.2.0",
    "shared": "workspace:^1.0.0"
  },
  "scripts": {
    "prebuild": "pnpm run -w build:package",
    "dev": "lerna run prebuild && dumi dev",
    "prepare": "dumi setup",
    "build": "lerna run prebuild && dumi build",
    "build:ci": "pnpm run -w build:package && dumi build"
  }
}
