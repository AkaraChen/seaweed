import { Meta, Canvas, Story } from '@storybook/addon-docs';
import { html } from 'lit';
import '@/link.ts';
import { targetArray } from '../src/util/type.ts';

# Link

```tsx
import '@akrc/seaweed/link.js';

<sw-link>text</sw-link>;
```

<Meta
    title="Common/Link"
    args={{
        text: 'Link',
        disabled: false,
        underline: false,
        href: 'https://bing.com',
        target: '_blank',
    }}
    argTypes={{
        target: {
            control: 'select',
            options: targetArray,
        },
    }}
/>

<Canvas>
    <Story name="Page">
        {({ text, disabled, underline, href, target }) =>
            html`
                <sw-link
                    ?disabled=${disabled}
                    ?underline=${underline}
                    href=${href}
                    target=${target}
                    >${text}</sw-link
                >
            `
        }
    </Story>
</Canvas>
