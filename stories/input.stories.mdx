import { Meta, Story, Canvas } from "@storybook/addon-docs";
import { html } from "lit";
import "@/input.ts";
import { typeArray, sizeArray } from "../util/type.ts";

# Alert

Alert is used to notify users of current state changes or features.

```tsx
import "@akrc/seaweed/input.js";

<sw-input></sw-input>;
```

<Meta
    title="Form/Input"
    args={{
        type: "normal",
        placeholder: "",
        size: "normal"
    }}
    argTypes={{
        type: {
            control: "select",
            options: [...typeArray, 'normal'],
        },
        size: {
            control: "select",
            options: sizeArray,
        },
    }}
/>

<Canvas>
    <Story name="Page">
        {({ type, placeholder, size }) =>
            html`
                <sw-input
                    type=${type}
                    placeholder=${placeholder}
                    size=${size}
                    ></sw-input>
            `
        }
    </Story>
</Canvas>
