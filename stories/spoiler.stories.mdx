import { Meta, Story, Canvas } from "@storybook/addon-docs";
import { html } from "lit";
import "@/spoiler.ts";

# Alert

Alert is used to notify users of current state changes or features.

```tsx
import "@akrc/seaweed/alert.js";

<sw-spoiler>Lorem ipsum dolor sit amet</sw-spoiler>;
```

<Meta
    title="Feedback/Spoiler"
    args={{
        event: "click"
    }}
    argTypes={{
        event: {
            control: "select",
            options: ["click", "hover"],
        },
    }}
/>

<Canvas>
    <Story name="Page">
        {({event}) =>
            html`<sw-spoiler event=${event}>Test</sw-spoiler>`
        }
    </Story>
</Canvas>
