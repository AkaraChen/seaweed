import { Meta, Story, Canvas } from "@storybook/addon-docs";
import { html } from "lit";
import "@/divider.ts";
import { sizeArray } from "../util/type.ts";

# Avatar

```tsx
import "@akrc/seaweed/avatar.js";

<sw-avatar src="path/to/image"></sw-avatar>;
```

<Meta
    title="Other/Divider"
    args={{
        text: 'lorem',
        position: 'medium',
        vertical: false,
        dashed: false,
        dotted: false
    }}
    argTypes={{
        position: {
            control: "select",
            options: ['left', 'medium', 'right'],
        },
    }}
/>

<Canvas>
    <Story name="Page">
        {({ text, position, vertical, dashed, dotted }) =>
            html`<sw-divider text=${text} position=${position} ?vertical=${vertical} ?dashed=${dashed} ?dotted=${dotted}></sw-divider>`
        }
    </Story>
</Canvas>
