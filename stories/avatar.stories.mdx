import { Meta, Story, Canvas } from "@storybook/addon-docs";
import { html } from "lit";
import "@/avatar.ts";
import { sizeArray } from "../util/type.ts";

# Avatar

```tsx
import "@akrc/seaweed/avatar.js";

<sw-avatar src="path/to/image"></sw-avatar>;
```

<Meta
    title="Data display/Avatar"
    args={{
        src: 'https://avatars.githubusercontent.com/akarachen',
        size: 'normal',
        rounded: true
    }}
    argTypes={{
        size: {
            control: "select",
            options: sizeArray,
        },
    }}
/>

<Canvas>
    <Story name="Page">
        {({ src, size, rounded }) =>
            html`<sw-avatar src=${src} size=${size} ?rounded=${rounded}></sw-avatar>`
        }
    </Story>
</Canvas>
