import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { html } from 'lit';
import '@/button.ts';
import { typeArray, sizeArray } from '@/util/type.ts';

# Button

```ts
import '@akrc/seaweed/button.js';

<sw-button>Button</sw-button>;
```

<Meta
    title="Button"
    args={{
        type: 'info',
        size: 'normal',
        primary: true,
        loading: false,
        disabled: false,
    }}
    argTypes={{
        type: {
            control: 'select',
            options: typeArray,
        },
        size: {
            control: 'select',
            options: sizeArray,
        },
    }}
/>

<Canvas>
    <Story name="Page">
        {({ type, size, primary, loading, disabled }) =>
            html`
                <sw-button
                    type=${type}
                    size=${size}
                    ?primary=${primary}
                    ?loading=${loading}
                    ?disabled=${disabled}
                    >Button</sw-button
                >
            `
        }
    </Story>
</Canvas>
