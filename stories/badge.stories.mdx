import { Meta, Story, Canvas } from "@storybook/addon-docs";
import { html } from "lit";
import "@/badge.ts";
import { typeArray } from "../util/type.ts";

# Badge

Badges are used to highlight a items status.

```tsx
import "@akrc/seaweed/badge.js";

<sw-badge>JavaScript</sw-badge>;
```

<Meta
    title="Data display/Badge"
    args={{
        type: "normal",
        text: "JavaScript",
        noborder: false,
        disabled: false,
    }}
    argTypes={{
        type: {
            control: "select",
            options: ["normal", ...typeArray],
        },
    }}
/>

<Canvas>
    <Story name="Page">
        {({ type, text, noborder, disabled }) =>
            html`
                <sw-badge
                    type=${type}
                    ?noborder=${noborder}
                    ?disabled=${disabled}
                    >${text}</sw-badge
                >
            `
        }
    </Story>
</Canvas>
