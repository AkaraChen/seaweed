import { Meta, Story, Canvas } from "@storybook/addon-docs";
import { html } from "lit";
import "@/alert.ts";
import { typeArray } from "../util/type.ts";

# Alert

Alert is used to notify users of current state changes or features.

```tsx
import "@akrc/seaweed/alert.js";

<sw-alert>Lorem ipsum dolor sit amet</sw-alert>;
```

<Meta
    title="Feedback/Alert"
    args={{
        type: "info",
        text: "Lorem ipsum dolor sit amet, consectetur adipiscing elit",
        title: "",
        fill: true,
        border: "none",
    }}
    argTypes={{
        type: {
            control: "select",
            options: typeArray,
        },
        border: {
            control: "select",
            options: ["none", "top", "left"],
        },
    }}
/>

<Canvas>
    <Story name="Page">
        {({ type, text, title, fill, border }) =>
            html`
                <sw-alert
                    type=${type}
                    title=${title}
                    ?fill=${fill}
                    border=${border}
                    >${text}</sw-alert
                >
            `
        }
    </Story>
</Canvas>
