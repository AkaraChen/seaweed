import { Meta, Story, Canvas } from "@storybook/addon-docs";
import { html } from "lit";
import "./switch.ts";
import { sizeArray } from "shared/type.ts";

# Switch

```tsx
import "@akrc/seaweed/switch.js";

<sw-switch></sw-switch>;
```

<Meta
    title="Form/Switch"
    args={{
        size: "normal",
        disabled: false,
        checked: false,
    }}
    argTypes={{
        size: {
            control: "select",
            options: sizeArray,
        },
    }}
/>

<Canvas>
    <Story name="Page">
        {({ size, disabled, checked }) =>
            html`<sw-switch
                size=${size}
                ?checked=${checked}
                ?disabled=${disabled}
            ></sw-switch>`
        }
    </Story>
</Canvas>
