import { Meta, Story, Canvas } from "@storybook/addon-docs";
import { html } from "lit";
import "./button.ts";
import { typeArray, sizeArray } from "shared/type.ts";

# Button

```tsx
import "@akrc/seaweed/button.js";

<sw-button>Button</sw-button>;
```

<Meta
    title="Common/Button"
    args={{
        type: "info",
        size: "normal",
        primary: true,
        loading: false,
        disabled: false,
        rounded: false
    }}
    argTypes={{
        type: {
            control: "select",
            options: typeArray,
        },
        size: {
            control: "select",
            options: sizeArray,
        },
    }}
/>

<Canvas>
    <Story name="Page">
        {({ type, size, primary, loading, disabled, rounded }) =>
            html`<sw-button
                    type=${type}
                    size=${size}
                    ?primary=${primary}
                    ?loading=${loading}
                    ?disabled=${disabled}
                    ?rounded=${rounded}
                    >
                    Button
                    </sw-button>`
        }
    </Story>
</Canvas>
