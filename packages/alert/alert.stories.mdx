import { Meta, Story, Canvas } from "@storybook/addon-docs";
import { html } from "lit";
import "./alert.ts";
import { typeArray } from "shared/type.ts";

# Alert

Alert is used to notify users of current state changes or features.

```tsx
import "@akrc/seaweed/alert.js";

<sw-alert>Lorem ipsum dolor sit amet</sw-alert>;
```

<Meta
    title="Feedback/Alert"
    args={{
        type: "info",
        text: "Lorem ipsum dolor sit amet, consectetur adipiscing elit",
        title: "",
        fill: true,
        border: "none",
        outline: false
    }}
    argTypes={{
        type: {
            control: "select",
            options: typeArray,
        },
        border: {
            control: "select",
            options: ["none", "top", "left"],
        },
    }}
/>

<Canvas>
    <Story name="Page">
        {({ type, text, title, fill, border, outline }) =>
            html`
                <sw-alert
                    type=${type}
                    title=${title}
                    ?fill=${fill}
                    border=${border}
                    ?outline=${outline}
                    >${text}</sw-alert
                >
            `
        }
    </Story>
</Canvas>
